feature: **Deck example (`examples/deck`)**: the radically skinned sibling of the soundboard — the same committed music catalog (real playback through the audio effect channel, queue, search, seek, transport, context-menu `pbcopy`) wearing vintage hi-fi rack-unit identity in the true two-window shape: a fixed player window (512x264, `resizable = false`, hidden-inset titlebar whose chrome-drawn enamel cap band is the `window_drag` region, traffic lights cleared by the live `on_chrome` inset) and a matching playlist unit as a model-declared second window (`windows_fn` + `window_view`, racked in/out by the chunky PL key or `primary+L`, user close round-tripping through `on_close`). The skin is a one-finish custom token set split by material — warm cream/putty enamel chassis around dark smoked-glass display bays printing in a single phosphor green, signal amber reserved for the queue and the failure stamps — plus a fixed-count `UiApp.chrome` display-list pass that machines the device from pure fills, hairlines, gradients, and paths (no bitmap skin assets): the cap band with its raised DECK brand plate, outer bevels, a ridged grip band, recessed corner screws, control wells, glass scanlines and glare, a seven-segment elapsed readout as sheared glowing paths, a five-stop phosphor band monitor (an honest VISUALIZER on equalizer-style frequency stops — the deck has no equalizer DSP, so nothing there pretends to be a control), and an analog rotary volume knob whose face draws over the real volume slider, its position dot derived from the same synced fraction. The main display carries a deterministic title marquee (a pure function of the playback clock, like the 32-band `ui.chart` spectrum and the band monitor), a `TRK NN` + timecode line, an honest bitrate/size readout computed from the manifest's real bytes, and the loaded record's cover in its own art bay; the playlist unit is one smoked-glass bay of numbered phosphor rows with right-aligned durations and the loaded row highlighted. The transport is the full five-key hardware row — prev / play / pause / stop / next, stop as honest halt-and-rewind, the square stop glyph shipped as a registered app icon (`app:stop`). `zig build test-example-deck` runs transport/queue/search dispatch through the fake effects executor, the playlist window's full round-trip through real dispatch (open by toggle, drive the ledger from the second canvas, close by Msg and as the user), spectrum + band-monitor + marquee determinism, cover registration and the codec-less degrade, chrome command-count stability, the one-finish + high-contrast-fallback theming contract, automation click-through on the five-key transport, and layout budgets at both fixed window sizes.
