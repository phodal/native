fix: **The spinner spins**: the widget never armed a render animation — it drew a static 8-spoke burst whose phase only moved if the app animated `value` by hand. It is now a muted circular track with an accent quarter-arc segment, and the runtime keeps a looping rotation render animation on the arc command (render animations gained a `.wrap` loop mode and an angle-sampled rotation channel — matrix lerp collapses a full turn — one linear 1s revolution per cycle). Refreshes preserve the rotation phase, an unmounted or hidden spinner has its animation removed so the view parks instead of pumping frames forever, and reduced motion renders the arc as a static pose.
- **Spinner metrics match the house bar**: the intrinsic size dropped from the 36px control square to the compact icon register — 16px (`sm`, the component default) / 20px (default) / 24px (`lg`), density-scaled — with a 2px stroke, so an inline spinner sits flush in a row of compact controls instead of reading below it. Deterministic static renders pose the arc from `value` (fractions of a turn), so previews and screenshots stay reproducible.
