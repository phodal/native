fix: **gate.sh derives the example registry from build.zig**: `registered_examples` was a hand-maintained second registry that drifted twice in one day (silently skipping new examples' suites in `gate fast`); the list is now extracted from build.zig's `addExampleTestStep` registrations at run time (POSIX `sed`, loud failure if the pattern matches nothing), so build.zig is the single source of truth.
