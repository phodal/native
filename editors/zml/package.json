{
  "name": "zml",
  "displayName": "ZML (Native SDK markup)",
  "description": "Syntax highlighting, diagnostics, completion, and hover for Native SDK .zml markup views.",
  "version": "0.1.0",
  "publisher": "native-sdk",
  "license": "MIT",
  "engines": {
    "vscode": "^1.75.0"
  },
  "categories": [
    "Programming Languages"
  ],
  "main": "./extension.js",
  "activationEvents": [
    "onLanguage:zml"
  ],
  "contributes": {
    "languages": [
      {
        "id": "zml",
        "aliases": [
          "ZML",
          "Native SDK markup"
        ],
        "extensions": [
          ".zml"
        ],
        "configuration": "./language-configuration.json"
      }
    ],
    "grammars": [
      {
        "language": "zml",
        "scopeName": "source.zml",
        "path": "./syntaxes/zml.tmLanguage.json"
      }
    ],
    "configuration": {
      "title": "ZML",
      "properties": {
        "zml.serverPath": {
          "type": "string",
          "default": "native",
          "description": "Path to the Native SDK CLI used to start the markup language server (`native markup lsp`). Set an absolute path if the CLI is not on PATH."
        }
      }
    }
  }
}
