feature: **GTK host window seams reach the AppKit contract**: app timers (markup hot-reload and press-hold now work on Linux), appearance events (dark scheme, reduce-motion, high-contrast from the toolkit settings), main-WebView layering (a layered shell app previously aborted at launch on Linux), window options at create (resizable, hidden titlebar styles, content min-size floors), pre-allocation frame reads that report the pending default size instead of a transient 0x0, and interactive window moves from the widget drag channel.
