feature: **Streaming audio with a verified track cache**: `fx.playAudio` now takes URL sources beside local paths and resolves them in a fixed honest order — local `path` first, then a verified cache entry at `cache_path` (played locally, no network), then a progressive stream (macOS: AVPlayer beside the local-file AVAudioPlayer) that starts playing while bytes are still arriving and fills the cache in parallel for the next play. `expected_bytes` is the integrity gate: cache entries and finished downloads are size-verified (download lands beside the final name and renames into place atomically), so a partial or stale entry never plays. Audio events gain an honest `buffering` flag (an un-paused stream waiting for bytes — distinct from playing and paused; seek and volume work mid-stream), the automation snapshot reports `state=buffering` and `source=local|cache|stream`, and `native_sdk.audioCachePath` keys the cache by URL hash under the platform caches directory (delete `<caches>/<app>/audio/` to clear it). Network failure mid-stream and offline-with-a-cold-cache each deliver one explicit `.failed` Msg; hosts with a player but no streaming path report `audio_streaming` unsupported and degrade the same way. Replay is unchanged by design: delivered audio events journal at the effect boundary, so streaming timing stays outside the recording and replay never touches the network (journal format v2 carries the new flag).
- **The music examples stream on demand**: the manifest gains `.url_base` (env-overridable with `NATIVE_SDK_MUSIC_URL_BASE`) and per-track `.bytes` (`tools/prepare-example-music.sh` emits both), and soundboard and deck resolve local file → cache → stream per play. The soundboard's bar shows "buffering…" and answers a dead stream with "stream unavailable — check the connection and try again"; the deck's VFD stamps `BUFFERING` and `STREAM LOST` with its own remedy line — distinct from the assets-not-prepared notices, because the fix is different. The null platform models URL sources, the assets-absent machine, stalls, and cache hits deterministically (a completed stream joins the fake cache), so the whole resolution order tests hermetically.
