fix: **Markdown mapper hardened against hostile input — three quadratic blowups fixed, one budget added**: paragraph/blockquote lines now join in a single measured allocation instead of re-formatting the accumulated text per line (a megabyte-long single paragraph used to demand gigabytes of arena), inline link/autolink parsing memoizes its forward closer scans (a wall of `[`, `<`, or `[a](x` units used to rescan to the terminator at every byte — minutes of CPU on a 256 KiB paste), and bare-URL trailing-paren trimming keeps its balance incrementally instead of recounting the URL per trimmed `)`. Joined paragraph text truncates deterministically at the new `max_markdown_paragraph_bytes` (8 KiB) budget, same convention as every other markdown capacity.
- **Hostile-input corpus joins the suite** (`markdown_hostile_tests.zig`): targeted fixtures for 100-deep quote/list/details nesting, gigantic tables, kilochar words, bracket/autolink/reference-link bombs, paren-tail URLs, unterminated fences, HTML soup, NUL bytes and invalid UTF-8 (rendered through the reference renderer), mixed RTL/CJK/emoji/ZWJ, and five-megabyte documents — plus a seed-pinned fuzz generator splicing those shapes. Every build asserts a linear arena bound, so a reintroduced quadratic join fails loudly.
