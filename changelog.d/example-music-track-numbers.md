improvement: **Track numbers in the prepared example music**: `tools/prepare-example-music.sh` now tags each prepared file with its track number — the 1-based position in the album's seeded, deterministic order, so it always matches the committed manifest — and the exact-tags verification covers the five-field set (title, artist, album, date, track) so nothing else survives the metadata strip.
