improvement: **Homepage proof for "Beautiful by default. Customizable by design."**: the Principles section now sets soundboard and deck side by side — the same player, two identities, every visible difference design tokens plus a chrome pass — and deck joins the showcase as the sixth tab. Deck gains the same env-gated `HOMEPAGE_SHOTS=1` offscreen shot renderer as the other showcase apps (one capture — the skin has one finish by design), and the soundboard pair is regenerated on the current engine.
