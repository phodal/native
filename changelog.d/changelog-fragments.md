improvement: **Changelog fragments (`changelog.d/`)**: concurrent branches no longer conflict on `CHANGELOG.md` — each change ships a small section-tagged fragment (`feature:`/`improvement:`/`fix:` first line, see `changelog.d/README.md`), and `scripts/changelog-merge.sh` folds all fragments into the Unreleased section (grouped by tag, sections created on demand) and deletes them during release prep.
