fix: **Docs live previews lay out with the selected pack's tokens**: the wasm preview host laid out widget trees with default tokens while painting with the active pack's, so pack-stated layout metrics — the Geist 32/40/48 control ladder, the 24px tabs gap, the button-group chip gap — never showed under the docs Geist toggle even though real apps honor them. Layout now reads the same resolved tokens as paint, and because pack and scheme swaps both route through the full rebuild, a toggle re-lays as well as repaints; default-pack tiles are pixel-identical before and after (verified headlessly across every scene in both schemes).
