improvement: **Windows hidden-titlebar fidelity**: `.hidden_inset`/`.hidden_inset_tall` windows now keep the full system frame with DWM-drawn min/max/close (snap layouts included) instead of coming up as bare borderless popups — the caption band is reclaimed for the app via `WM_NCCALCSIZE`, markup `window-drag` regions answer `WM_NCHITTEST` as `HTCAPTION` (drag, double-click maximize, right-click system menu), and the window-chrome channel reports the caption-button cluster as a trailing inset so headers pad the correct edge per platform.
