improvement: **The interaction seams are documented where developers look**: Native UI gains a keyboard-routing section (the two focus registers, the four-step key precedence ending in `Options.on_key`, and the quiet-list-row rule with the soundboard as the worked example), its Messages section documents `on_double_press` (runtime-derived click counts, the additive press-then-double firing order) and row-level Enter as a `list-item`'s primary action, the List component page grows a Selection and activation recipe, and the `on-submit` hover/vocab doc names the list-row behavior beside the text-entry one.
