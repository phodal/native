improvement: **Dark-gray default app icon**: the generated default icon's plate (what `native init` scaffolds and what packaging falls back to for apps that ship no icon) moves from the blue-violet gradient to the dark-neutral family — #262626 falling to #171717, the dark scheme's surface steps — so an iconless app gets a quiet neutral plate instead of a hue that could read as its brand. Regenerated via `zig build generate-icon`: `assets/icon.{icns,png,ico,svg}` and the CLI's embedded scaffold/packaging copies (`src/tooling/default_icon.{icns,png}`).
