feature: **Real text in the reference renderer**: a bounded, std-only TTF parser (`canvas.font_ttf`: glyf/loca/cmap format 4/hmtx, simple + composite glyphs, fixed budgets, comptime-validated) over the bundled Geist Regular face feeds real outlines through the vector core, so screenshots, `render_pixels`, mobile embeds, and every headless frame get readable anti-aliased text instead of placeholder block glyphs. Layout is untouched — line breaking, widget sizing, carets, and every measurement golden still come byte-identical from the deterministic estimator; only glyph painting changed. Unmapped codepoints (emoji, missing text bytes) and over-budget glyphs keep the historical block fallback, and macOS windows keep native AppKit text.
- **Estimator-aligned pen positions**: unshaped text runs now paint each cluster at the same per-character estimator advance layout measured with (the block painter stepped a fixed 0.5em per scalar and could overrun measured line bounds). Mono and the weight/italic span ids currently paint with the regular outlines at their own estimator advances — GeistMono and weighted faces are not bundled yet; painting stays honest about that rather than faking bold.
- Regenerated pixel goldens whose frames draw text: `markdown_tests` dev2-README signature, `reference_tests` proxy-text pixels, `text_tests` wrap pixels, and the gpu-dashboard/gpu-components example reference signatures. Structural and command goldens did not move.
