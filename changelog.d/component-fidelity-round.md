improvement: **Component fidelity**: progress renders as a 4px fully rounded rail on the muted track; pagination sits on a 2px gap with ghost pages, chevron previous/next, an ellipsis cell, and an outline current page whose currency shows through the variant (selected outline/ghost NAV buttons no longer wear a permanent hover wash — toggles keep it); and the `toggle` element is now honestly a pressed-state button (the sliding on/off control is `switch` alone).
- **gpu-components header**: the band drops its self-title and carries a real three-trigger theme strip (Light/Dark/High, wired per mode) beside Refresh, and the environment picker floats as a true anchored menu below its trigger.
- **Skeleton pulse**: mounted skeletons pulse (an engine-armed looping opacity oscillation on the placeholder fill, phase-preserving across refreshes, removed on unmount so idle views park; reduce-motion renders them static) — the same seam that spins the spinner.
- **Badge + table registers**: badges compact to the 20px pill (smaller text, borderless secondary, quiet destructive wash with destructive ink); tables and data grids drop per-cell boxes for engine-drawn hairline row separators (none under the last row), full-width row hover/selection washes, tight cell padding on comfortable rows, and `text-alignment="end"` numeric columns — system-monitor's process table now rides the real table vocabulary.
