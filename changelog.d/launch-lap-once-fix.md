fix: **`lapOnce` launch laps fire once per NAME, not once per process**: the once-guard's nested container ignored its comptime name parameter, so the compiler deduplicated it across instantiations and the first lap fired in a process suppressed every other `lapOnce` site (`first_present_recorded` never printed). The container now references the name, giving each lap its own guard.
