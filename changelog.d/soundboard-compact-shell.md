feature: **Soundboard compact shell**: the soundboard now carries one codebase and two shells — the desktop composition unchanged, plus a phone composition (stacked touch header, single-column grid, full-surface album detail with back navigation, an always-visible now-playing mini bar) selected in the root view by a width-derived form factor, with safe-area padding consumed from the window-chrome channel and a mobile embed entry so `native dev --target ios` runs it on the simulator.
