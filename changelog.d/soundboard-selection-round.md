improvement: **Soundboard selection round**: track rows follow the desktop list convention — single click selects (an accent fill under window-background ink), double click plays, arrows move the selection without playback or a focus ring, and Enter plays it; the scrubber interpolates the playing position every presented frame between the player's coarse ticks (never rewinding mid-motion, zero frames while paused); the playing row's state icon names the play state; the Albums/Songs switcher became a flush button group; album tiles dropped their hover wash; the transport's play button joined the quiet register; and the waveform mark trails the now-playing title.
