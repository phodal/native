fix: **Collect-mode stderr survives the child reap**: the real effect executor now joins its stderr tail reader BEFORE waiting on the child — `wait` closes the child's pipes, and a reader still in flight raced that close and delivered an empty tail (Linux lost the tail consistently; the same race existed on macOS behind friendlier scheduling). Env-gated golden/screenshot test dumps also compile without libc now, so the module suites pass on Linux.
