fix: **Automation select/edit/focus actions publish their completing frame**: `selectAutomationCanvasWidget`, `editAutomationCanvasWidgetText`, and automation focus changes repainted without recording a GPU-surface input timestamp, so the frame they presented completed as a "steady-state" frame and never republished observable state — the automation snapshot's `gpu_frame` only advanced when a PREVIOUS interaction happened to leave a dangling pending input for that frame to resolve, a timing accident that loaded macOS CI runners lose (`menu item automation select did not request a GPU frame` in the gpu-components smoke). These actions now record the automation input like every other interaction path, so their completing frame resolves it into a recorded input latency and invalidates the runtime for republish, occluded or visible.
