fix: **Budget overflows can no longer tear the retained widget tree**: `copyWidgetLayoutTree` resets the per-view pools (text bytes, spans, context-menu items, chart series/points) and then copies node by node, so a pool overflow mid-loop left a PARTIAL tree applied — the next frame rendered half a view and interaction state resolved against it until the next successful dispatch. The pool budgets are now pre-validated in one pass (charges mirrored exactly from the copy path, including runtime-reconciled editor text) before anything resets, making every teaching error atomic: the previous tree stays applied, the error is loud, the copy-path checks remain as structural backstops. Same count-before-copy pattern the display-list side already followed.
- **Budget-edge matrix joins the suite** (`canvas_budget_edge_tests.zig`): a real `UiApp` driven to every reachable `canvas_limits` boundary — layout nodes, retained widget text (via span links), per-frame display-list text, inline spans, declared context-menu items, anchored surfaces, chart series, chart points (via band series, which charge values + low) — pinning at-budget applies, one-past fails with the named teaching error, nothing half-applies, and the next sane rebuild recovers.
