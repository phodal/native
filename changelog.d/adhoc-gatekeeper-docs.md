improvement: **The signing docs state what Gatekeeper actually shows**: the code-signing page now documents the ad-hoc experience honestly — a quarantined download shows the standard "cannot check it for malicious software" dialog with the right-click → Open bypass, scp/USB/local copies carry no quarantine attribute and launch with no dialog, and a "damaged" verdict means a broken resource seal, never the expected ad-hoc flow.
