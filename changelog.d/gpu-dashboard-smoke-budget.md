fix: **gpu-dashboard smoke no longer flakes on machine load**: the 150 ms first-frame assertion now honors `ZN_SMOKE_BUDGET_MS` and relaunches the app once on a budget-only overrun before failing — every correctness assertion (frame presented, packet-representable, retained content, widget semantics) stays strict, and the runtime's own 150 ms budget verdict is still asserted verbatim whenever the measured latency is within budget.
