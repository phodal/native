improvement: **Hidden-inset titlebars across the showcase**: notes, soundboard, system-monitor, kanban, ui-inbox, feed, habits, and gpu-dashboard adopt the tall hidden-inset treatment — each app's header row IS the titlebar (window drag region, traffic lights inline, leading inset via `on_chrome`), replacing the plain OS bar. Chrome-geometry pin tests per app; the notes folder rail shows Rename by presence instead of a permanently disabled button.
