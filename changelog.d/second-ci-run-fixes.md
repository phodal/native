fix: **GPU smoke ready ceilings scale with `ZN_SMOKE_BUDGET_MS`**: the gpu-surface/gpu-dashboard/gpu-components smokes' hard 500 ms automation-ready ceiling (which a noisy macos-14 runner blew at 576 ms) now rises with `ZN_SMOKE_BUDGET_MS` (never below the 500 ms local default), the gpu-surface and gpu-components first-frame ceilings honor the same budget with the dashboard's honest-verdict rule, and the macOS WebView CI job sets `ZN_SMOKE_BUDGET_MS=1500` — local defaults and every correctness assertion stay strict.
- **Linux Canvas Smoke starts inside the wait window**: the CI job sets `GTK_A11Y=none` (under xvfb the runner's session bus has no `org.a11y.Bus`, and GTK4's accessibility init blocked ~25 s on the GDBus lookup, landing the app's first runtime event after `automate wait`'s 30 s window) and captures `automate wait` output from stderr where the CLI actually prints it — the old stdout-only `| grep -q 'ready=true'` could never match and would have failed the step right after readiness.
