feature: **Calculator example (`examples/calculator`)**: a showcase four-function calculator in a fixed 320x496 window — the classic keypad grid (exact 66x54 keys, double-width zero, the pending operator highlighted from the model), a live expression + result display with the last calculation remembered above it, and a near-monochrome graphite theme with one calculator-orange accent in both modes. Immediate-execution arithmetic on f64 with honest formatting (12-digit entries, display-only rounding, scientific beyond the window, divide-by-zero shows Error with the failing expression), keyboard input through a real expression `text_field` (digits/operators/backspace/enter via the widget keyboard path) plus an Escape chrome shortcut for AC, and the plain-form TEA update — no effects at all. Markup-first: the entire keypad and header are comptime-compiled `.native`; the only Zig view is the scaled result paragraph. `zig build test-example-calculator` runs exhaustive arithmetic through `msgForPointer` on every key, real `gpu_surface_input` keyboard events, shortcut and theming paths, markup engine parity, automation snapshot assertions, and an exact-frame keypad layout check.
