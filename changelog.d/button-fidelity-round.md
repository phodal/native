feature: **Button fidelity round**: the button family (`button`, `icon_button`, `toggle_button`, `toggle`) now carries the full measured house register. Metrics: the shared control ladder lands on whole-pixel 28/32/36 heights (`sm`/default/`lg`; `icon` stays the default square) — buttons, inputs, and select triggers move together, so a mixed toolbar row keeps exactly one height; the side inset holds one 10px register across the ladder; corners sit at 10 (the lg radius token) with one deliberate step down to 8 at `sm`; the label is 14px medium at default/lg and steps to 12.8 at `sm` (the compact rung is a genuinely smaller control, not the same control squeezed); the inline icon gap is 6 at every step. Labels draw with the medium companion face via the new `typography.button_font_id` token (null resolves to the built-in medium id while the house sans is in play, follows a theme's custom `font_id` otherwise; hosts without a mapped medium face keep regular outlines at the same measured advances). States: filled variants press one wash step past hover (80%/90% alpha over one base color, scheme-free); quiet variants press to the deeper neutral wash while a toggle's on-state keeps the muted wash and selected NAV buttons keep bare chrome; disabled fades fill, border, and ink to half strength — the same wash the selection controls wear — so a disabled ghost no longer grows a gray box and a disabled primary still reads as a primary. Buttons are FLAT: no variant casts a shadow (the measured base register has none), and the invalidation budget drops the shadow halo with it.
- **Destructive is the quiet red chip**: a translucent destructive wash (10% alpha in light, 20% in dark — stated per scheme in the new themed control tables, never sniffed from surface luminance) under text in the destructive red itself, with no border; hover and pressed deepen the wash one 5% step at a time, and disabled keeps the half-strength register.
- **Dark outline is glass**: white at 4.5% for the body under a white 15% border, so the control brightens whatever it floats over; light outline is the opaque register (page-background fill under the standard hairline). Select triggers inherit the treatment through their existing outline fallback.
