fix: **The iOS example project links again**: the embed static library's panic symbolication references `_dyld_get_image_header_containing_address`, which the iOS SDK marks unavailable — the Xcode example now compiles the same documented `dladdr` shim the mobile-canvas host already carries, so `xcodebuild` succeeds against a fresh `zig build lib -Dtarget=aarch64-ios-simulator` library. The repo-root embed lib also picks up the x86_64 Debug LLVM-backend workaround `addMobileLib` already applied, so fixed-shell libraries built for Intel simulators or x86_64 Android emulators no longer hand hosts corrupted viewport floats.
