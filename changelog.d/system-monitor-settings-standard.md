improvement: **System monitor lands the standard settings flow and a one-height toolbar**: Settings now opens only the standard way — the app-menu item's primary+comma shortcut, registered in `app.zon` (`.shortcuts`) and mapped through `on_command` to one `.open_settings` code path — so the toolbar's Settings chip is gone; the settings window is redesigned to the grouped-form register (one row: the sampling switch with its cadence facts, applying live with no Apply step), retitled plain "Settings", sized to exactly its content, fixed-size, and stripped of the close-this-window copy. The toolbar now sits on ONE size register (`.sm` button, filter field, and sort toggles all render one control height; the pause chip is a real button with the play/pause icon inline), and the same one-register-per-row rule fixed the markdown viewer's toolbar (`text-field` joins its sm buttons) and the soundboard header (search matches the sm tabs). New tests cover the shortcut command mapping and platform dispatch, idempotent open, the settings form's live switch, and a toolbar layout check pinning every control to one height.
