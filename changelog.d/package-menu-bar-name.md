fix: **Packaged apps show their display name in the menu bar**: the generated Info.plist put the lowercase manifest name in `CFBundleName`, which is exactly the key the application menu reads — the menu bar said `soundboard` while the Dock and Gatekeeper said `Soundboard`. `CFBundleName` now carries the display name like every other user-facing surface; the manifest name remains the executable's name.
