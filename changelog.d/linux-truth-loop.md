improvement: **Linux live-truth loop** (`tools/linux-truth/`): a containerized Xvfb harness that builds every showcase app for Linux, drives real per-app scenarios (clicks, typing, wheel scrolling, resizes) over the automation channel, captures engine and X-level screenshots, and runs the engine + example suites on real GTK — sources mounted read-only, all artifacts container-local.
