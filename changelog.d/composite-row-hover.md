fix: **Composite rows hover as one surface**: hover, the pointer cursor, and the pressed wash now resolve through the same fall-through walk presses take (`widgetHoverTargetForHit`), so a two-line list row keeps its wash and pointer cursor across its own title, snippet, gaps, padding, and trailing whitespace — no more per-child dead zones. Links keep their own hover inside pressable rows, bare text keeps its selection affordance, and the raw pressed id still drives text drag-selection.
